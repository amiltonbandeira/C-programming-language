#include <stdio.h> // This adds or includes the standard input/output library informations in the module
int main() { //This creates the/a new function called main with no arguments values
		printf("Hello World!\n");//This calls the printf function to display a message
}//This closes the program

